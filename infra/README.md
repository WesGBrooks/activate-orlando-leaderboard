# Deploy notes

Primary path is **Railway** using the root `Dockerfile` + `railway.toml`.

AWS App Runner / Lambda Function URLs would also work from the same Dockerfile,
but are intentionally not the default for this friends app.
